package jessy;

public class DimLight extends Light
{
   public DimLight(String label, String gads)
   {
      super(label, gads);
   }
   
   public DimLight(String label, String gads, String type)
   {
      super(label, gads, type);
   }
}

